@page
@using EShopOnAbp.CurrencyService.Pages
@model IndexModel
@using Microsoft.AspNetCore.Mvc.Localization
@using EShopOnAbp.CurrencyService.Localization
@using Volo.Abp.Users
@inject IHtmlLocalizer<CurrencyServiceResource> L
@inject ICurrentUser CurrentUser

<abp-card>
    <abp-card-header>Welcome Web Host Page Bahaa</abp-card-header>
    <abp-card-body>
        @if (!CurrentUser.IsAuthenticated)
        {
            <form method="POST">
                <input type="submit" asp-page-handler="Login" value="LOGIN" class="btn btn-primary" />
            </form>
        }
        else
        {
            <strong>Claims</strong><br />
            @Html.Raw(CurrentUser.GetAllClaims().Select(c => $"{c.Type}={c.Value}").OrderBy(x => x).JoinAsString(" <br /> "))
        }
        <hr />
        <p class="text-end"><a href="https://abp.io?ref=tmpl" target="_blank">abp.io</a></p>
    </abp-card-body>
</abp-card>
